package com.company.repository;

import com.company.utils.ManagerHR;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ManagerHRRepository extends JpaRepository<ManagerHR, Long> {
}
